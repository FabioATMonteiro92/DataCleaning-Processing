# Local data — allow small sample files in these folders
# (We still ignore other random data paths)
data/                # keep ignoring a generic data folder if you create one
# Do NOT ignore CSV/XLSX globally anymore

# If you ever add big raw dumps elsewhere, ignore them specifically:
# raw_data/
# *.parquet